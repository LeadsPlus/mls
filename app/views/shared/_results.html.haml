.sorting_options.float-right= sortable "price", "Required Payments"
.spacer
%div
  - if @matches.any?
    %table
      %tbody
        = render :partial => 'shared/house', :collection => @matches

    = paginate @matches

  - else
    We're sorry but no houses match that search