// TODO because sometimes this can be counties too, I'm going to have to change the word 'towns'
$('#towns_list ul').eq(0).append("<%= escape_javascript( render partial: 'shared/search/location_item', collection: @towns) %>");
$('#search_town').val('');